namespace APPBD2;

public enum ContainerType
{
    Liquid = 'L',
    Gas = 'G',
    Cooling = 'C'
}

public enum LoadRiskLevel
{
    Normal,
    Dangerous
}

public enum LoadType
{
    Fuel,
    Food
}